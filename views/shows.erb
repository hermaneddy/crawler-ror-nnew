<!DOCTYPE html>
<html>
<link rel="stylesheet" type="text/css" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
<body>

<h2>Concert List NY</h2>

<!-- Table -->
<table class="table">
        <thead>
          <tr>
            <th>Date</th>
            <th>Event</th>
            <th>Time</th>
            <th>Price</th>
          </tr>
        </thead>
        <tbody>
      <% @concerts.each do |concert| %>
      <tr>
      <% if concert.at_css('.ticket-price .custom').nil? %>
      		<td><%= concert.at_css('.dates').text.strip %></td>
            <td><a href="<%= concert.at_css('a.tickets')[:href]%>"><%= concert.at_css('.vevent').text.strip%></a></td>
            <td><%= concert.at_css('.times').text.strip
%></td>
            <td><%= concert.at_css('.price-range').text.strip%></td>
      <% else %>
      		<td><%= concert.at_css('.dates').text.strip %></td>
            <td><del><%= concert.at_css('.vevent').text.strip%></del></td>
            <td><%= concert.at_css('.times').text.strip
%></td>
            <td>SOLD OUT</td>
      <% end %>
          
         
          </tr>
      <% end %>    
        </tbody>
      </table>
</body>
</html>














